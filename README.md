# Лабораторна робота №7. Основи класифікації 
Мета: засвоїти основні підходи до розв’язання задачі класифікації
засобами Python.

# ХІД РОБОТИ
1. Імпортовано необхідні бібліотеки для роботи з машинним навчанням, обробкою даних та оцінюванням моделей:
   scikit-learn
   pandas

3. Завантажено вбудований набір даних digits, який містить зображення рукописних цифр від 0 до 9.

5. Проведено ознайомлення з даними:

   виведено список класів;
   переглянуто ознаки набору даних;
   створено таблицю DataFrame;
   отримано описову статистику даних.

6. Дані було розділено на:

   тренувальну вибірку — 60%;
   тестову вибірку — 40%.

7. Проведено навчання та тестування таких моделей машинного навчання:

   Logistic Regression;
   Decision Tree;
   Random Forest;
   K-Nearest Neighbors (KNN);
   Support Vector Machine (SVM);
   Gaussian Naive Bayes.

8. Для кожної моделі:

   виконано навчання методом fit();
   отримано прогноз методом predict();
   обчислено метрики:

   Accuracy;
   Precision;
   F1-score;
   побудовано матрицю помилок (confusion_matrix);
   сформовано детальний звіт класифікації (classification_report).

9. Отримані результати було збережено у підсумкову таблицю для подальшого порівняння ефективності моделей.

10. Проведено аналіз результатів та визначено моделі, які найкраще розпізнають рукописні цифри.
